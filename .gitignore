/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Credenciales de sesión de WhatsApp (Baileys) — NO subir
/.wa-auth

# App móvil (va en otro repo)
/mobile

# Dumps de base de datos (datos reales de clientes: NO subir)
/backups
*.dump
*.sql.gz
